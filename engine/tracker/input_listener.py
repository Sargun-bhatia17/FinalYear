"""
input_listener.py — Global Input Delta Capture
================================================
Uses pynput to silently track:
  - keystroke_count  (count only — no key content recorded)
  - mouse_click_count
  - scroll_delta_y

Privacy guarantee: no sensitive key data is ever stored or transmitted.
"""

# TODO: Task Sequence 1 — implement pynput global listeners
