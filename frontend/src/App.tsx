// App.tsx — Root React Application
// Connects to the Python IPC bridge via WebSocket and
// renders the main dashboard layout.

// TODO: Task Sequence 4 — implement WebSocket provider + routing

export default function App() {
  return (
    <div>
      {/* AttentionLens Dashboard — placeholder */}
    </div>
  );
}
